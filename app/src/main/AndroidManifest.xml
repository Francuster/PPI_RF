<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
<!--        <service-->
<!--            android:name=".service.FaceRecognitionService"-->
<!--            android:enabled="true"-->
<!--            android:exported="true"></service>-->

        <activity
            android:name=".OfflineFaceRecognitionActivity"
            android:exported="false"
            android:label="@string/title_activity_offline_face_recognition"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegistroExitosoAntesalaActivity"
            android:label="Registro Exitoso Antesala" />

        <activity android:name=".CameraIngresoEgresoActivity" android:label="Escanea" />
        <activity android:name=".AnteEscaneaActivity" android:label="AnteEscanea" />
        <activity android:name=".FormularioOfflineActivity" android:label="FormularioOffline" />
        <activity android:name=".RegistroDenegadoActivity" android:label="RegistroDenegado" />
        <activity android:name=".CameraLoginActivity" android:label="CameraLoginActivity" />
        <activity android:name=".InicioSeguridadActivity" android:label="InicioSeguridadActivity" />
        <!-- Declara tu servicio dentro de la sección <application> -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".RegistroExitosoActivity"
            android:label="RegistroExitoso" />

        <activity
            android:name=".activity.CameraxActivity"
            android:label="CameraX" /> <!-- Declara tu servicio dentro de la sección <application> -->
        <service
            android:name=".utils.NetworkChangeService"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".utils.NetworkChangeReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
